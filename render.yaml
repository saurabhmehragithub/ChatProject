services:
  - type: web
    name: chat-application
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/chat-application-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
